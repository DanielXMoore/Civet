import assert from "assert"
import TSService from "../source/lib/typescript-service"

describe("ts stuff", function () {
  it("should parse tsconfig", function () {
    assert(true);
    TSService()
  });
});
