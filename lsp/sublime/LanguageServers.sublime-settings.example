// Snippet to merge into your Sublime Text LSP package Server Configurations.
// Open: Preferences -> Package Settings -> LSP -> Server Configurations
{
    "civet-lsp": {
        "enabled": true,
        "command": ["civet-lsp", "--stdio"],
        "selector": "source.civet",
    }
}
