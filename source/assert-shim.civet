// Minimal node:assert replacement for browser bundles.
// This needs to be separate from browser-shim.civet
// because the default export is a function.

export default function assert(value: unknown, message = 'Assertion failed'): asserts value
  throw new Error(message) unless value
