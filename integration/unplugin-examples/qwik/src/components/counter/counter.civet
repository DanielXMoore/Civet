import { component$, useSignal } from "@builder.io/qwik"

export Counter := component$ ->
  count := useSignal 0
  <div>
    <p>Count from Civet component: {count.value}</p>
    <button type="button" onClick$={=> count.value++}>
      Increment
    </button>
